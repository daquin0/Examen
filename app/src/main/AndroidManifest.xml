<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dcom.triviamultilenguaje">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TriviaMultilenguaje">
        <activity
            android:name=".activities.questions.ResultActivity"
            android:exported="true" />
        <activity
            android:name=".activities.questions.QuestionActivity3"
            android:exported="true" />
        <activity
            android:name=".activities.questions.QuestionActivity2"
            android:exported="true" />
        <activity
            android:name=".activities.login.Login"
            android:exported="true" />
        <activity
            android:name=".activities.questions.MainActivity"
            android:exported="true" />
        <activity
            android:name=".activities.splash.SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>